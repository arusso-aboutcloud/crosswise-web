{
  "name": "crosswise-web",
  "private": true,
  "version": "0.1.0",
  "description": "Browser-based Entra ID toxic permission combination scanner",
  "type": "module",
  "scripts": {
    "dev": "vite",
    "build": "vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "deploy": "npx wrangler pages deploy dist --project-name=crosswise-web"
  },
  "dependencies": {
    "@azure/msal-browser": "^5.11.0",
    "three": "^0.184.0"
  },
  "devDependencies": {
    "vite": "^8.0.14",
    "vitest": "^3.2.2",
    "wrangler": "^4.93.0"
  }
}
