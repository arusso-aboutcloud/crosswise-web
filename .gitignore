node_modules/
dist/
build/
.wrangler/
.env
.env.*
*.log
.DS_Store
Thumbs.db
*.key
*.pem
.claude/

# Cloudflare raw API exports — local only, never committed
docs/security/cf-rules-dump.json

# Operator-specific Cloudflare config — never commit
infra/cf-apply-config.local.json

# Cloudflare rules export — operator inventory, never commit
docs/security/cloudflare-rules.md
